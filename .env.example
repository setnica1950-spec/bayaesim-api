# Optional HTTP port. Defaults to 3000.
PORT=3000

# Enable the OMAX debug endpoint only for local development.
NODE_ENV=development
OMAX_DEBUG_ENABLED=false

# Required only when OMAX_DEBUG_ENABLED=true.
OMAX_CLIENT_ID=replace-with-client-id
OMAX_CLIENT_SECRET=replace-with-client-secret
